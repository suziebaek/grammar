# 버그 수정 내역 (앱 코드 부분)

이번 수정에서는 **난이도 루브릭 관련 개선(D축 배정/검증, 목표-응답 대조 검증)은 제외**하고,
앞서 리뷰에서 발견된 **명확한 코드 버그 5건**만 고쳤습니다. 난이도 루브릭 개선은 다음 단계에서 다룹니다.

---

## 1. `app.py` — 구글 네이티브 키 사용 시 모델 선택 무시 버그
- **문제**: 사이드바에서 `google/gemini-2.5-flash-lite`를 선택해도, Google AI Studio 키(`AIzaSy...`)로
  생성/재생성할 때는 두 곳 모두 `"gemini-3.1-pro-preview"`가 하드코딩되어 있어 항상 그 모델이 호출됨.
  도움말 문구("시험가동 시 flash-lite 선택")와 실제 동작이 어긋나 있었고, 의도치 않게 비싼 모델이 계속
  호출되는 비용 문제가 있었음.
- **수정**: `google_model_name` 변수를 새로 만들어 `selected_model`(사이드바 선택값)에서 `google/` 접두어만
  떼어내 사용하도록 변경. 생성 1차 호출과 반려 문항 재생성 호출 두 군데 모두 적용.

## 2. `app.py` — "생성 기록" 탭의 세트별 .txt 다운로드가 엉뚱한 내용을 받던 버그
- **문제**: `tab3`(생성 기록)의 "세트 N 다운로드 (.txt)" 버튼이 `tab1`에서만 정의되는 전역 변수 `set_text`를
  그대로 재사용하고 있었음. 그 결과:
  1. 히스토리의 어떤 세트를 눌러도 항상 **가장 최근에 생성한 세트**의 내용이 다운로드됨 (`.docx` 버튼은
     `h`를 올바르게 참조해서 문제없었음).
  2. "🗑️ 결과 초기화" 버튼으로 `pending`을 비운 뒤 히스토리 탭에서 다운로드를 시도하면 `set_text`가
     정의된 적이 없어서 **`NameError`로 앱이 죽음**.
- **수정**: 현재 순회 중인 세트 `h`를 기준으로 `set_text_this`를 새로 조립해서 사용하도록 변경. 이제
  `tab1`의 상태와 무관하게 각 세트가 자기 자신의 내용을 정확히 다운로드함.

## 3. `app.py` — "난이도 활성화(상/중/하)" 토글이 아무 효과도 없던 버그
- **문제**: `btn_high` / `btn_mid` / `btn_low` 토글 3개가 있었지만 생성 로직 어디에서도 이 값을 읽지
  않아서, 토글을 꺼도 옆의 숫자 입력값이 그대로 반영되어 문제가 생성됨 (선생님이 "상 끄기"를 눌러도
  실제로는 상 문제가 계속 나오는 상태였음).
- **수정**: 토글 값을 `enable_high/mid/low` 변수로 받아서, 꺼진 난이도는 문항 수를 강제로 0으로 만들도록
  변경. 또한 꺼진 난이도의 숫자 입력창도 함께 비활성화해서 "꺼져 있는데 숫자가 활성화되어 있어
  헷갈리는" 상태를 없앴습니다.

## 4. `excel_exporter.py` — 학기 표시가 "2026-가을"로 하드코딩되어 있던 버그
- **문제**: 엑셀 원고 내보내기 시 `"semester": "2026-가을"`이 고정값이라, 2026년 가을 학기가 지나도
  계속 같은 값이 찍힘.
- **수정**: `_current_semester_label()` 헬퍼 함수를 추가해 실행 시점의 연도/월을 기준으로
  `{연도}-{봄|가을}`을 동적으로 계산하도록 변경 (3~7월=봄/1학기, 8~2월=가을/2학기, 국내 학사일정 근사치).

## 5. `app.py` / `validator.py` — 죽은 코드 정리
- **문제**: `app.py` 상단에서 `validator.py`의 `validate_question_llm`, `validate_batch_llm`을
  `# <--- 이 줄이 반드시 있어야 합니다!` 라는 주석과 함께 import하고 있었지만, 실제로는 어디서도
  호출되지 않는 구버전 함수였음 (실제 검수는 `app.py` 안의 `validate_batch_json`이 담당). 다음 사람이
  "어느 게 진짜 검증 로직이지?" 헷갈릴 수 있는 상태였음.
- **수정**: `app.py`에서 사용하지 않는 import를 제거. `validator.py` 파일 자체는 하위 호환을 위해
  남겨두되, 상단에 "사용 중단 — 실제 검수는 validate_batch_json이 담당" 안내 주석을 추가. 필요 없다고
  판단되면 이 파일은 지워도 앱 동작에 영향 없음.

---

## 이번에 건드리지 않은 것 (다음 단계)
- 난이도 루브릭 D(지문형식)축을 실제로 배정/검증하는 로직 추가
- 배정된 목표(A/B/C) vs 최종 응답값 대조 검증 추가
- README를 실제 구조(구글 시트 기반, E/H 레벨, 검증기 등)에 맞게 재작성
- `questions_db.json` / `concept_hierarchy.json` / `update_db.py`의 위치 재정의(현재 앱 미사용 상태)
- 구글 시트 URL이 소스에 평문 하드코딩되어 있는 부분(리포를 public으로 올릴 경우 노출 위험)
